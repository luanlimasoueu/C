#include <stdio.h> // Inclui a biblioteca de entrada e saída padrão

int main() {
    // printf() exibe o texto na tela
    printf("Hello, World!\n"); 
    
    return 0; // Indica que o programa foi finalizado com sucesso
}
